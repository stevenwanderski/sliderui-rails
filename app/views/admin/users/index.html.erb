<h1>Users</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Date</th>
      <th>Email</th>
      <th>Sliders</th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= l(user.created_at) %></td>
        <td><%= user.email %></td>
        <td>0</td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @users, theme: 'twitter-bootstrap-4' %>
